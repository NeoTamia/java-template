name: Lint Action

on:
  push:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/lint.yml'
      - '**.gradle'
      - '**.kts'
      - 'gradle/**'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/lint.yml'
      - '**.gradle'
      - '**.kts'
      - 'gradle/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/v')}}

jobs:
  lint:
    name: Linting
    uses: NeoTamia/actions/.github/workflows/jvm-lint.yml@ed83480bdf97a9588379118210702681f1be328e # main
    permissions:
      contents: read
      issues: write
      pull-requests: write
      checks: write
