name: Test Action

on:
  push:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/test.yml'
      - 'gradle/**'
      - '**.gradle'
      - '**.kts'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/test.yml'
      - 'gradle/**'
      - '**.gradle'
      - '**.kts'
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/v')}}

jobs:
  test:
    name: Test
    uses: NeoTamia/actions/.github/workflows/jvm-test.yml@da0f4095741fc1138ecdf68cc9efa6be0e6ab59c # main
    permissions:
      checks: write
      pull-requests: write
    secrets: inherit
    with:
      codecov: true
