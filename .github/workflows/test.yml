name: Test Action

on:
  push:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/test.yml'
      - 'gradle/**'
      - '**.gradle'
      - '**.kts'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '.github/workflows/test.yml'
      - 'gradle/**'
      - '**.gradle'
      - '**.kts'
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/v')}}

jobs:
  test:
    name: Test
    uses: NeoTamia/actions/.github/workflows/jvm-test.yml@ed83480bdf97a9588379118210702681f1be328e # main
    permissions:
      checks: write
      pull-requests: write
    secrets: inherit
    with:
      codecov: true
